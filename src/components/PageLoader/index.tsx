import { LoaderWrapper } from './styles';
import LogoLoader from '../../commons/LogoLoader';

const PageLoader = () => {
  return (
    <LoaderWrapper>
      <LogoLoader size='md' />
    </LoaderWrapper>
  );
}

export default PageLoader;